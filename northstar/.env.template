# Auth — shared JWT secret. MUST be identical across ALL modules or auth fails.
# Generate once, copy the same value everywhere (see vault/.env).
JWT_SECRET=

# NorthStar — Knowledge Hub
# No LLM calls needed yet — data collection only
# When Ollama is set up, uncomment to enable AI-powered insights:
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=gemma3:4b
